package org.learn_java.bot.commands;

public enum CommandType {
	// TODO implement ROLE command type loading probably wrap Command in a context object to allow multiple types
	OWNER, MODERATOR, ROLE, ANY
}
